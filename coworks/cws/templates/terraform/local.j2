# ---------------------------------------------------------------------------------------------------------------------
# LOCALS FOR THIS MICROSERVICE
# ---------------------------------------------------------------------------------------------------------------------

locals {
  {{ resource_name }}_security_group_ids = {{ security_groups | list | replace("'", '"') }}
  {{ resource_name }}_subnet_ids = {{ subnets | list | replace("'", '"')}}
}
