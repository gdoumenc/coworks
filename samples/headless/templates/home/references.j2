{% macro references(references, reference_ids) -%}

    {% if references.visible %}
        <!-- References -->
        <section id="features">
            <div class="container">
                <header>
                    <h2>{{ references.header_h2 }}</h2>
                </header>
                <div class="row aln-center">
                    {% for ref in references.references if ref.slug in reference_ids %}
                        <div class="col-4 col-6-medium col-12-small">

                            <!-- Feature -->
                            <section>
                                <a href="#" class="image featured"><img src="https://cdn.cosmicjs.com/{{ ref.image }}"
                                                                        alt="{{ ref.title_h2 }}"/></a>
                                <header>
                                    <h3>{{ ref.title_h2 }}</h3>
                                </header>
                                {{ ref.content }}
                            </section>

                        </div>
                    {% endfor %}
                </div>
            </div>
            <div class="col-12">
                <ul class="actions">
                    <li><a href="#" class="button icon solid fa-file">{{ references.button }}</a></li>
                </ul>
            </div>
        </section>
    {% endif %}

{%- endmacro %}
