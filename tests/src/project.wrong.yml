version: 2
services:
  - module: example
    services:
      - project1
      - project2
commands:
  test:
    class: test_cmd.test_cmd
    b: value
    a: default
    services:
      - module: example
        service: project2
        a: project2
        workspaces:
          - workspace: prod
            a: prod2
      - module: example
        service: autre
        a: autre1
